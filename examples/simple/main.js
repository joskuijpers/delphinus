require("./foo"); // More tests
const link = require("link");

/*
let running = true;
while (running) {
    // Handle events
    let events = __delphinus.do_events();

    if (events == -1) {
        running = false;
        break;
    }

    // Flip screen
}
*/
